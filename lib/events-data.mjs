// DATA ONLY — curated by hand from verified research and filled in separately. Nothing in here is
// generated, guessed or fetched: every entry is added by a human who checked the source, and
// CALENDAR_ASOF records the day that check last happened. The engine in lib/events.mjs must work
// for any dataset, including this empty one, so keep all logic out of this file.
// Entry shape: { id, name, domain: 'sports' | 'science', topic, start, end, blurb, whyQuiz,
// sourceUrl, headline? } — see lib/events.mjs for the sanitiser that enforces it.
export const CALENDAR_ASOF = '2026-09-13';
export const EVENTS = [];
