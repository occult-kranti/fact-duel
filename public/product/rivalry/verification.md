# FACT//DUEL rivalry pass: release verification

12 September 2026. Published only after the following gates pass.

| Gate | Evidence |
|---|---|
| Automated tests | 58 tests passed, 0 failed. Existing engine/HTTP/bot/timing/coin/profile tests plus closed-history secrecy, five-round deduplication, multi-round recovery, local concern editing/reset/migration and truthful presentation rules. |
| TypeScript | `tsc --noEmit` passed against the final implementation. |
| Production build | Sites production build succeeded before this version was saved. The optional Three.js chunk can still produce a size warning; no device-performance claim follows from a build. |
| Advisor review | Two bounded source passes. Nine findings repaired or mitigated in source. No remaining reviewed code blocker; source inspection is distinct from customer observation. |
| Contrast repair | Light-theme draw title changed from 1.85:1 against white to approximately 6.06:1. This checks that pair, not full accessibility conformance. |
| Original art | One 1672×941 generated still, inspected as an asset and converted to a 120,928-byte WebP. Exact prompt retained. No competitor art or branded team imagery imported. |
| Audience | Existing owner-private access preserved. Coins remain free per-room simulations with no cash value. |

Not run: agent browser, physical-device flows, rendered 320–430px QA, screen reader, 200% zoom observation, graphics-driver loss, frame/battery measurement, distant human screens, or customer retention/learning tests. Managed Sites preview requires an explicit request for browser, visual or end-to-end testing; no alternative browser-control path was improvised. Source changes shorten the active mobile layout but do not establish a particular phone's actual fit or input behavior.

All historical competitor evidence remains the original 32-observation purposive sample across eight selected apps, not a new representative survey. Current documents clearly separate this implementation from historical v3/v4 design records.

The same polling/casual timing architecture remains. Completed history slightly increases room state/response size, bounded by five rounds. No new capacity or WAN stress test is claimed. Before expanding access, observe request costs and real response behavior using the existing scale roadmap.
